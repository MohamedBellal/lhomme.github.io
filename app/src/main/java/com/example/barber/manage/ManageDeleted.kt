package com.example.barber.manage

import android.content.Context
import android.widget.Toast

class ManageDeleted {

    fun handleDeleted(context: Context) {
        // Logique pour gérer les rendez-vous supprimés
        Toast.makeText(context, "Gestion des rendez-vous supprimés", Toast.LENGTH_SHORT).show()
        // Ajoute ici la logique de gestion des rendez-vous supprimés.
    }
}

package com.example.barber

import android.content.Context
import android.widget.Toast

class ManageServices {

    fun handleServices(context: Context) {
        // Logique pour gérer les services
        Toast.makeText(context, "Gestion des services", Toast.LENGTH_SHORT).show()
        // Ajoute ici la logique de gestion des services.
    }
}
